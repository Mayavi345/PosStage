<UserControl x:Class="PosStage.MVVM.View.ProductSlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PosStage.MVVM.View" 
             mc:Ignorable="d" 
             Name="Product" >

    <Grid Background="#304158" Margin="5" HorizontalAlignment="Center" Width="120" Height="170">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <Ellipse Height="80" Width="80" Margin="0,10" HorizontalAlignment="Center">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding Source, UpdateSourceTrigger=PropertyChanged}" />
                </Ellipse.Fill>
            </Ellipse>
            <TextBlock Text="{Binding Product.Name, UpdateSourceTrigger=PropertyChanged}" FontWeight="SemiBold" FontSize="14" Margin="0,5" TextAlignment="Center"/>
            <TextBlock Text="{Binding Product.Price}" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <Button Style="{StaticResource buttonCount}" Content="+" Command="{Binding AddCartCommand}" Background="#6589B8" BorderBrush="{x:Null}" Foreground="White" Width="30" Height="30" Margin="0,5"/>
        </StackPanel>
    </Grid>
</UserControl>
